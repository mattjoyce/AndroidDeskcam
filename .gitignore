backend/build/
*.keystore
.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
frontend/go/deskcam

# The luminary panel write-ups. Kept on disk, because several kanban cards cite them as
# the record of why a decision was made, and out of the repository on purpose.
reviews/

# Bench captures, sidecars, and local analysis outputs
deskcam-bracket-*
deskcam-sweep-*
deskcam-walk-*
deskcam-noisefloor.json
deskcam-scale.json
lin/
burst/
*.jpg
*.jpeg
*.dng
*.thumb.jpg

# The handoff note for the next agent. Written fresh each session, and out of the
# repository on purpose: it is the state of a bench, not of the code.
HANDOFF.md

# OS and editor files
.DS_Store
*.swp
*~


# release builds, made by scripts/release.sh
dist/

# The graphify knowledge graph, rebuilt from the sources on demand
graphify-out/
