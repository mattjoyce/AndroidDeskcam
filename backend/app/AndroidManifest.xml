<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.deskcam">

    <!-- The version comes from VERSION at the top of the repository; build.sh injects it. -->

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 17 gates LAN traffic separately from internet access. Without this the
         app can reach the internet but every local-network packet is dropped, in both
         directions, which is exactly what a bench camera needs. -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <!-- The pairing callback is a plain HTTP request to the workstation. Android blocks
         outbound cleartext by default from targetSdk 28, so the call fails with
         "Cleartext HTTP traffic not permitted". This whole tool is HTTP on a trusted LAN
         by design; refer to the non-goals in docs/DECISIONS.md. -->
    <application
        android:usesCleartextTraffic="true"
        android:label="DeskCam"
        android:icon="@android:drawable/ic_menu_camera"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/DeskCam"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Pairing. A QR code holds deskcam://pair?cb=... and opens this activity
                 directly. A plain http URL cannot be used, because the browser enforces
                 HTTPS first and refuses it. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="deskcam" />
            </intent-filter>
        </activity>

        <!-- Levelling the mount. Not exported: nothing outside the app has any reason to
             open it, and an exported activity is a surface to defend for no gain.
             Portrait is locked because the screen is used while it is nearly flat, which
             is exactly where the platform's own rotation cannot tell which way is up; a
             card whose two axes swapped under the bubble mid-adjustment would be worse
             than no card. -->
        <activity
            android:name=".LevelActivity"
            android:exported="false"
            android:theme="@style/DeskCam"
            android:screenOrientation="portrait"
            android:label="Level" />

        <service
            android:name=".CamService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
